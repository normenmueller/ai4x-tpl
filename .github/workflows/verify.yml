name: verify

on:
  push:
    branches: [trunk]
  pull_request:
    branches: [trunk]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run verify
        run: make verify
